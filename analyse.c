#include <stdio.h>

int main(int argn, char *argv[], char *arge[]) {
    //printf("%d\n", argn);
    if(argn < 2){
        printf("Un argument est requis\n");
        return 2;
    } else {
        //if(fichier introuvable) return 1;
        //else prog; return 0;
    }
    return 0;
}